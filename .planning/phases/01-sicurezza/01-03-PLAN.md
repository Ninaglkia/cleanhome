---
phase: 01-sicurezza
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/delete-account/index.ts
  - supabase/functions/auto-cancel-expired-bookings/index.ts
autonomous: true
gap_closure: true
requirements: [SEC-02, SEC-04]

must_haves:
  truths:
    - "delete-account catch block non restituisce raw err.message al client — solo stringa generica fissa"
    - "auto-cancel-expired-bookings catch block del query error non restituisce raw queryErr.message al client — solo stringa generica fissa"
    - "Entrambe le funzioni mantengono console.error server-side con il messaggio reale per diagnostica"
  artifacts:
    - path: "supabase/functions/delete-account/index.ts"
      provides: "Catch block con messaggio generico (no raw err.message al client)"
      contains: "Account eliminato"
    - path: "supabase/functions/auto-cancel-expired-bookings/index.ts"
      provides: "Query error catch con messaggio generico (no raw queryErr.message al client)"
      contains: "Errore interno del server"
  key_links:
    - from: "catch block delete-account"
      to: "json response"
      via: "return json()"
      pattern: "return json\\(\\{\\s*error:"
    - from: "queryErr check auto-cancel-expired-bookings"
      to: "Response body"
      via: "return new Response"
      pattern: "Errore interno del server"
---

<objective>
Chiudere il gap SEC-02: due Edge Functions fuori scope del piano 01-01 espongono ancora raw error message al client. Fix: sostituire i valori raw nei return client-facing con stringhe generiche fisse, mantenendo il console.error server-side. Rideploy automatico via MCP Supabase.

Purpose: Il success criterion 2 della fase (nessuna Edge Function espone informazioni interne al client) richiede che TUTTE le funzioni siano clean. delete-account e auto-cancel-expired-bookings erano fuori scope dell'audit originale e rimangono vulnerabili.

Output: Due Edge Functions corrette e ridesploy ate su Supabase. SEC-04 documentato come human verification post-lancio.
</objective>

<execution_context>
@/Users/ninomarianolai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ninomarianolai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- delete-account/index.ts — catch block attuale (linee 68-71): -->
```typescript
// PRIMA (da correggere):
  } catch (err: any) {
    console.error("[delete-account]", err?.message);
    return json({ error: err?.message ?? "Delete failed" }, 500);
  }

// DOPO:
  } catch (err: any) {
    console.error("[delete-account]", err?.message);
    return json({ error: "Impossibile eliminare l'account. Riprova più tardi." }, 500);
  }
```

<!-- auto-cancel-expired-bookings/index.ts — queryErr check attuale (linee 60-63): -->
```typescript
// PRIMA (da correggere):
  if (queryErr) {
    console.error("[auto-cancel] query error:", queryErr.message);
    return new Response(JSON.stringify({ error: queryErr.message }), { status: 500 });
  }

// DOPO:
  if (queryErr) {
    console.error("[auto-cancel] query error:", queryErr.message);
    return new Response(JSON.stringify({ error: "Errore interno del server. Riprova più tardi." }), { status: 500 });
  }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix raw error exposure in delete-account e auto-cancel-expired-bookings, poi rideploy</name>
  <files>
    supabase/functions/delete-account/index.ts,
    supabase/functions/auto-cancel-expired-bookings/index.ts
  </files>
  <action>
**File 1 — supabase/functions/delete-account/index.ts**

Trovare il catch block alle linee 68-71 e sostituire SOLO il return statement del catch:

PRIMA (esatto):
```typescript
  } catch (err: any) {
    console.error("[delete-account]", err?.message);
    return json({ error: err?.message ?? "Delete failed" }, 500);
  }
```

DOPO:
```typescript
  } catch (err: any) {
    console.error("[delete-account]", err?.message);
    return json({ error: "Impossibile eliminare l'account. Riprova più tardi." }, 500);
  }
```

Non modificare nient'altro nel file.

---

**File 2 — supabase/functions/auto-cancel-expired-bookings/index.ts**

Trovare il queryErr check alle linee 60-63 e sostituire SOLO il return della Response:

PRIMA (esatto):
```typescript
  if (queryErr) {
    console.error("[auto-cancel] query error:", queryErr.message);
    return new Response(JSON.stringify({ error: queryErr.message }), { status: 500 });
  }
```

DOPO:
```typescript
  if (queryErr) {
    console.error("[auto-cancel] query error:", queryErr.message);
    return new Response(JSON.stringify({ error: "Errore interno del server. Riprova più tardi." }), { status: 500 });
  }
```

Non modificare nient'altro nel file — in particolare NON toccare il catch block interno al loop (linee 128-132) che costruisce il results array per uso interno del cron job, che non è una response di errore client-facing.

---

**Verificare con grep che le modifiche abbiano effetto:**

```bash
grep -n "err?.message\|err\.message\|queryErr\.message" \
  /Users/ninomarianolai/CleanHomeRN/supabase/functions/delete-account/index.ts \
  /Users/ninomarianolai/CleanHomeRN/supabase/functions/auto-cancel-expired-bookings/index.ts \
  | grep -v "console\."
```

Il grep NON deve trovare nessuna riga (tutti i raw message rimasti sono solo in console.error).

---

**Rideploy via MCP Supabase:**

Eseguire i due deploy nell'ordine:

1. `mcp__supabase__deploy_edge_function` con:
   - `function_name`: `delete-account`
   - `verify_jwt`: `true`
   - (entrypoint dal file supabase/functions/delete-account/index.ts)

2. `mcp__supabase__deploy_edge_function` con:
   - `function_name`: `auto-cancel-expired-bookings`
   - `verify_jwt`: `false`
   - (entrypoint dal file supabase/functions/auto-cancel-expired-bookings/index.ts)

Il deploy di auto-cancel-expired-bookings ha verify_jwt=false perché viene chiamata dal cron pg_net con service-role Bearer, non da JWT utente.
  </action>
  <verify>
    <automated>grep -n "err?.message\|err\.message\|queryErr\.message" /Users/ninomarianolai/CleanHomeRN/supabase/functions/delete-account/index.ts /Users/ninomarianolai/CleanHomeRN/supabase/functions/auto-cancel-expired-bookings/index.ts | grep -v "console\." || echo "CLEAN — nessun raw message esposto in risposte client"</automated>
  </verify>
  <done>
- delete-account: catch restituisce solo `{ error: "Impossibile eliminare l'account. Riprova più tardi." }` — nessun raw err.message
- auto-cancel-expired-bookings: queryErr check restituisce solo `{ error: "Errore interno del server. Riprova più tardi." }` — nessun raw queryErr.message
- Entrambe le funzioni hanno console.error server-side invariati
- Entrambe le funzioni ridesployate su Supabase (delete-account verify_jwt=true, auto-cancel verify_jwt=false)
  </done>
</task>

</tasks>

<verification>
```bash
# Verifica che nessun raw message sia più esposto in risposte client (solo console.error è OK)
grep -n "err?.message\|err\.message\|queryErr\.message" \
  /Users/ninomarianolai/CleanHomeRN/supabase/functions/delete-account/index.ts \
  /Users/ninomarianolai/CleanHomeRN/supabase/functions/auto-cancel-expired-bookings/index.ts \
  | grep -v "console\." || echo "PASS — no raw messages in client responses"

# Verifica che le stringhe generiche siano presenti
grep -n "Impossibile eliminare" /Users/ninomarianolai/CleanHomeRN/supabase/functions/delete-account/index.ts
grep -n "Errore interno del server" /Users/ninomarianolai/CleanHomeRN/supabase/functions/auto-cancel-expired-bookings/index.ts
```
</verification>

<success_criteria>
1. `delete-account` catch block: ritorna solo `{ error: "Impossibile eliminare l'account. Riprova più tardi." }` — zero raw message
2. `auto-cancel-expired-bookings` queryErr check: ritorna solo `{ error: "Errore interno del server. Riprova più tardi." }` — zero raw queryErr.message
3. Tutti i `console.error` con messaggi reali sono mantenuti server-side
4. Entrambe le funzioni ridesployate su Supabase con le configurazioni JWT corrette
5. SEC-02 fully satisfied: tutte le Edge Functions non espongono raw error message al client
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/01-sicurezza/01-03-SUMMARY.md` con:
- Files modificati e le righe cambiate (before/after)
- Conferma deploy Supabase per entrambe le funzioni
- Status SEC-02: SATISFIED (tutte le Edge Functions ora clean)
- Nota SEC-04: La configurazione Sentry in app/_layout.tsx è corretta (verificata in 01-VERIFICATION.md). La verifica "riceve eventi e source maps" richiede human verification dopo il primo EAS production build — non è un gap di codice. Da eseguire manualmente: fare un EAS build con EXPO_PUBLIC_ENV=production, triggerare un errore nell'app, verificare in sentry.io che l'evento appaia con source maps (stack trace in TypeScript, non offuscato). SEC-04 va considerato PARTIALLY SATISFIED pending questa verifica post-lancio.
</output>

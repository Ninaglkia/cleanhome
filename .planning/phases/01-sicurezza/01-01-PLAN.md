---
phase: 01-sicurezza
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/stripe-subscription-create/index.ts
  - supabase/functions/stripe-booking-action/index.ts
  - supabase/functions/stripe-webhook/index.ts
  - supabase/functions/stripe-booking-payment/index.ts
autonomous: true
requirements: [SEC-01, SEC-02]

must_haves:
  truths:
    - "Una chiamata a stripe-subscription-create che genera un errore restituisce al client solo il campo `error` con messaggio generico — nessun campo `debug` ne' `env_check` nella risposta"
    - "Nessuna Edge Function ritorna raw error message, key prefix, o env mode al client in nessun caso di errore (500 o altro)"
    - "I log server-side (console.error) nei catch block contengono informazioni diagnostiche sufficienti per debugging senza esporle al client"
  artifacts:
    - path: "supabase/functions/stripe-subscription-create/index.ts"
      provides: "Catch block senza debug/env_check"
      contains: "Impossibile creare l'abbonamento"
    - path: "supabase/functions/stripe-booking-action/index.ts"
      provides: "Catch block con messaggio generico (no raw err.message al client)"
      contains: "Errore interno del server"
    - path: "supabase/functions/stripe-webhook/index.ts"
      provides: "Error response senza leak del messaggio Stripe"
      contains: "Webhook Error"
  key_links:
    - from: "catch block stripe-subscription-create"
      to: "json response"
      via: "return json()"
      pattern: "return json\\(\\{\\s*error:"
    - from: "catch block stripe-booking-action"
      to: "json response"
      via: "return json()"
      pattern: "Errore interno del server"
---

<objective>
Rimuovere il debug leak critico da stripe-subscription-create e fare audit di tutte le Edge Functions per assicurarsi che nessuna esponga informazioni interne (raw error, key prefix, env mode) al client in risposta a errori.

Purpose: Un attaccante che triggera errori nelle Edge Functions non deve sapere se l'ambiente e' test o live, non deve ricevere messaggi di errore Stripe raw, e non deve ricevere prefix delle chiavi segrete.

Output: Quattro Edge Functions con catch block hardened che logano server-side ma restituiscono solo messaggi generici al client.
</objective>

<execution_context>
@/Users/ninomarianolai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ninomarianolai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- Pattern attuale da rimuovere in stripe-subscription-create (linee 245-267): -->
```typescript
// PRIMA (da rimuovere):
} catch (err: any) {
  const msg = err?.message ?? String(err);
  console.error("[stripe-subscription-create error]", msg);
  return json(
    {
      error: "Impossibile creare l'abbonamento. Riprova più tardi.",
      debug: msg,                          // <-- RIMUOVERE
      env_check: {                         // <-- RIMUOVERE tutto il blocco
        has_secret_key: !!STRIPE_SECRET_KEY,
        has_price_id: !!STRIPE_LISTING_PRICE_ID,
        price_id_prefix: STRIPE_LISTING_PRICE_ID?.slice(0, 12) ?? null,
        secret_key_mode: STRIPE_SECRET_KEY?.startsWith("sk_live_")
          ? "live"
          : STRIPE_SECRET_KEY?.startsWith("sk_test_")
          ? "test"
          : "unknown",
      },
    },
    500
  );
}

// DOPO (pattern corretto):
} catch (err: any) {
  const msg = err?.message ?? String(err);
  console.error("[stripe-subscription-create]", msg);
  return json({ error: "Impossibile creare l'abbonamento. Riprova più tardi." }, 500);
}
```

<!-- Pattern attuale stripe-booking-action (linee 279-282) — ha leak di err.message al client: -->
```typescript
// PRIMA:
} catch (err: any) {
  console.error("[stripe-booking-action]", err?.message ?? err);
  return json({ error: err?.message ?? "Errore interno" }, 500);  // <-- err.message esposto
}

// DOPO:
} catch (err: any) {
  console.error("[stripe-booking-action]", err?.message ?? err);
  return json({ error: "Errore interno del server. Riprova piu' tardi." }, 500);
}
```

<!-- Pattern attuale stripe-webhook (linee 87-90) — leak di err.message nel body: -->
```typescript
// PRIMA:
} catch (err: any) {
  console.error("[stripe-webhook] signature error:", err?.message);
  return new Response(`Webhook Error: ${err?.message}`, { status: 400 });  // <-- message esposto
}

// DOPO (Stripe stessa si aspetta un body qualsiasi su 400, non serve il messaggio):
} catch (err: any) {
  console.error("[stripe-webhook] signature error:", err?.message);
  return new Response("Webhook signature verification failed", { status: 400 });
}
```

<!-- stripe-webhook handler error (linea 367-369) — leak di err.message: -->
```typescript
// PRIMA:
} catch (err: any) {
  console.error("[stripe-webhook] handler error:", err?.message ?? err);
  return new Response(`Handler Error: ${err?.message}`, { status: 500 });
}

// DOPO:
} catch (err: any) {
  console.error("[stripe-webhook] handler error:", err?.message ?? err);
  return new Response("Internal server error", { status: 500 });
}
```

<!-- stripe-booking-payment e' gia' OK (linee 304-308) — solo verifica: -->
```typescript
} catch (err: any) {
  const msg = err?.message ?? String(err);
  console.error("[stripe-booking-payment]", msg);
  return json({ error: "Impossibile avviare il pagamento. Riprova più tardi." }, 500);
}
// Questo e' corretto — nessun leak.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rimuovere debug/env_check da stripe-subscription-create</name>
  <files>supabase/functions/stripe-subscription-create/index.ts</files>
  <action>
Nel catch block finale (attorno alla linea 245-267):

1. Eliminare il campo `debug: msg` dalla risposta JSON
2. Eliminare l'intero blocco `env_check: { ... }` dalla risposta JSON
3. Mantenere il `console.error("[stripe-subscription-create error]", msg)` — e' corretto, e' server-side
4. Il return finale deve essere solo: `return json({ error: "Impossibile creare l'abbonamento. Riprova più tardi." }, 500);`
5. Mantenere TUTTO il resto del file invariato — solo il catch block va modificato

Il commento `// Pre-launch: return the underlying error...` va rimosso insieme ai campi debug.

Risultato atteso del catch block:
```typescript
} catch (err: any) {
  const msg = err?.message ?? String(err);
  console.error("[stripe-subscription-create error]", msg);
  return json(
    { error: "Impossibile creare l'abbonamento. Riprova più tardi." },
    500
  );
}
```
  </action>
  <verify>
    <automated>grep -n "debug\|env_check\|key_mode\|key_prefix\|has_secret_key\|has_price_id" /Users/ninomarianolai/CleanHomeRN/supabase/functions/stripe-subscription-create/index.ts | grep -v "^.*:.*\/\/" || echo "CLEAN — no debug fields found"</automated>
  </verify>
  <done>Il grep non trova piu' nessun campo debug o env_check nel file. Il catch block ritorna solo `{ error: "Impossibile creare l'abbonamento. Riprova più tardi." }`.</done>
</task>

<task type="auto">
  <name>Task 2: Audit e fix dei catch block nelle altre tre Edge Functions</name>
  <files>
    supabase/functions/stripe-booking-action/index.ts,
    supabase/functions/stripe-webhook/index.ts
  </files>
  <action>
**stripe-booking-action** (catch block ~linea 279-282):
- Il campo `error: err?.message ?? "Errore interno"` espone il raw error message al client
- Sostituire con: `return json({ error: "Errore interno del server. Riprova piu' tardi." }, 500);`
- Mantenere il `console.error` con il messaggio reale per logging server-side

**stripe-webhook** — due catch block:

1. Signature check catch (~linea 87-90):
   - `return new Response(\`Webhook Error: ${err?.message}\`, { status: 400 })` espone il messaggio
   - Sostituire con: `return new Response("Webhook signature verification failed", { status: 400 });`
   - Mantenere `console.error("[stripe-webhook] signature error:", err?.message);`

2. Handler error catch (~linea 367-369):
   - `return new Response(\`Handler Error: ${err?.message}\`, { status: 500 })` espone il messaggio
   - Sostituire con: `return new Response("Internal server error", { status: 500 });`
   - Mantenere `console.error("[stripe-webhook] handler error:", err?.message ?? err);`

**stripe-booking-payment**: Gia' clean — solo verificare visivamente che il catch non esponga campi extra (gia' confermato: restituisce solo `{ error: "Impossibile avviare il pagamento. Riprova più tardi." }`). Nessuna modifica necessaria.

Non modificare nessun'altra parte del codice — solo i return statement dei catch block.
  </action>
  <verify>
    <automated>grep -n "err?.message\|err.message" /Users/ninomarianolai/CleanHomeRN/supabase/functions/stripe-booking-action/index.ts /Users/ninomarianolai/CleanHomeRN/supabase/functions/stripe-webhook/index.ts | grep "return\|Response\|json" || echo "CLEAN — no raw err.message in responses"</automated>
  </verify>
  <done>
- stripe-booking-action: catch ritorna solo `{ error: "Errore interno del server. Riprova piu' tardi." }`
- stripe-webhook: entrambi i catch ritornano stringhe fisse senza interpolazione di err.message
- stripe-booking-payment: invariato e gia' corretto
  </done>
</task>

</tasks>

<verification>
Dopo entrambi i task, eseguire:

```bash
# Nessun campo debug/env_check in nessuna delle Edge Functions
grep -rn "debug:\|env_check:\|key_mode\|key_prefix\|has_secret_key" \
  /Users/ninomarianolai/CleanHomeRN/supabase/functions/ \
  --include="*.ts" || echo "PASS — no debug fields"

# Nessun err.message esposto direttamente in response body (solo in console.error)
grep -rn "err?.message\|err\.message" \
  /Users/ninomarianolai/CleanHomeRN/supabase/functions/stripe-booking-action/index.ts \
  /Users/ninomarianolai/CleanHomeRN/supabase/functions/stripe-webhook/index.ts \
  | grep -v "console\." || echo "PASS — no raw messages in client responses"
```
</verification>

<success_criteria>
1. `stripe-subscription-create` catch ritorna solo `{ error: "Impossibile creare l'abbonamento. Riprova più tardi." }` — zero campi extra
2. `stripe-booking-action` catch ritorna solo `{ error: "Errore interno del server. Riprova piu' tardi." }` — non il raw `err.message`
3. `stripe-webhook` due catch ritornano stringhe fisse senza interpolazione
4. `stripe-booking-payment` verificata e invariata (gia' clean)
5. Tutti i `console.error` server-side sono mantenuti per diagnostica
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/01-sicurezza/01-01-SUMMARY.md` con:
- Files modificati
- Campi rimossi da ciascun catch block
- Status di ciascuna Edge Function (clean / fixed)
- Eventuali anomalie trovate durante l'audit
</output>
